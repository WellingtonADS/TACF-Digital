Usando DATABASE_URL (POSIX / Linux / macOS):
Usando psql com PGPASSWORD (POSIX):
PowerShell (Windows):
PowerShell com PGPASSWORD:
Alternativa sem alterar owner (conceder execução ao role):
Notas rápidas:

O comando ALTER FUNCTION ... OWNER TO requer superuser ou permissão para SET ROLE "supabase_admin". Se a conta usada não tiver essa permissão, use a alternativa GRANT EXECUTE.
Substitua <db_user>, <db_pass>, <host>, <port>, <db_name> pelos valores do seu ambiente.



admin_novo@fab.mil.br

12345678

inserir limite 

avido via e-mail

bloquer sessão sessão

enviar o comprovante para o email

gerar lista de chamada

inspeção de saúde atrasada, 

setor, saram, nome de guerra, setor

1 taf / 2 taf

troca senha 6 digitos

email de solicitação/ aprovando a troca

autentica em 2 fatores







SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

DATABASE_URL=

SEED_ADMIN_EMAIL=
SEED_ADMIN_PASSWORD=




