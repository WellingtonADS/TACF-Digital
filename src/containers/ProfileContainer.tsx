import Layout from "@/components/layout/Layout";

export const ProfileContainer = () => {
  return (
    <Layout>
      <h2 className="text-xl font-semibold">Profile</h2>
      <p>Profile container placeholder</p>
    </Layout>
  );
};

export default ProfileContainer;
