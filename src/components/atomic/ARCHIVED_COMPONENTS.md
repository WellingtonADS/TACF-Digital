# Arquivados: Componentes Atômicos

Este arquivo documenta componentes atômicos movidos para arquivamento por
não estarem sendo utilizados no código (`src/`). Objetivo: evitar código morto
no diretório ativo de componentes e garantir que só implementamos o que é
realmente usado.

Arquivados:

- `src/components/atomic/archived/SidebarItem.tsx` — componente de item de
  sidebar. Não foi encontrado nenhum import no projeto; mantido em arquivo
  arquivado para referência.
- `src/components/atomic/archived/QuickActionCard.tsx` — cartão de ação rápida.
  Não foi encontrado nenhum import no projeto; mantido arquivado.

Próximos passos:

- Se houver intenção de usar qualquer componente arquivado, mova-o de volta
  para `src/components/atomic/` e atualize os locais que o consomem.
- Se após 30 dias não houver uso, considerar remoção permanente.
