# src skeleton

Este diretório é um esqueleto inicial para a reescrita do frontend.

Conselhos:

- Cada PR deve ser pequeno (1–2 páginas por PR).
- Não mover regras de negócio para o cliente; use RPCs em `supabase/rpc/`.
- Antes de abrir PR: `yarn && yarn lint && npx tsc --noEmit`.
